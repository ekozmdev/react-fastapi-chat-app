# ユーザー登録・認証機能 実装計画

## 1. 実装方針

### 認証方式
- **JWT (JSON Web Token)** ベースの認証システム
- FastAPIの `Depends` を活用したDependency Injection
- パスワードハッシュ化：bcrypt使用
- トークン有効期限：24時間（設定可能）

### ユーザー管理方式
- **Pythonスクリプトによるユーザー管理**: CLI/管理スクリプトでユーザー情報の登録・メンテナンスを実施
- **認証必須**: ゲストモードは削除し、全機能で認証を必須とする
- **ユーザー別データ分離**: チャット履歴はユーザーごとに完全分離

### セキュリティ要件
- パスワード強度チェック（最低8文字、英数字含む）
- メールアドレス重複チェック
- トークン検証とリフレッシュ機能
- CORS設定の見直し

## 2. データベーススキーマ変更

### 新規テーブル: `users`
```sql
CREATE TABLE users (
    id VARCHAR PRIMARY KEY DEFAULT gen_random_uuid()::text,
    email VARCHAR UNIQUE NOT NULL,
    username VARCHAR UNIQUE NOT NULL,
    password_hash VARCHAR NOT NULL,
    is_active BOOLEAN DEFAULT true,
    created_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP,
    updated_at TIMESTAMP DEFAULT CURRENT_TIMESTAMP
);
```

### 既存テーブル変更: `conversations`
```sql
-- user_id外部キーを追加（NOT NULL制約）
ALTER TABLE conversations ADD COLUMN user_id VARCHAR NOT NULL;
ALTER TABLE conversations ADD CONSTRAINT fk_conversations_user_id 
    FOREIGN KEY (user_id) REFERENCES users(id) ON DELETE CASCADE;

-- インデックス追加
CREATE INDEX idx_conversations_user_id ON conversations(user_id);
```

### マイグレーション戦略
- 既存データは削除または管理者ユーザーに紐付け
- 全ての会話は必ずuser_idを持つ（認証必須のため）

## 3. バックエンドAPI設計

### 新規依存関係
```bash
# uv addコマンドで追加（バージョン指定なし）
uv add "python-jose[cryptography]"  # JWT処理
uv add "passlib[bcrypt]"            # パスワードハッシュ化
uv add "python-multipart"           # フォームデータ処理
```

### 新規エンドポイント

#### 認証関連
```python
POST /api/auth/login             # ログイン（ユーザー登録はスクリプトで実施）
POST /api/auth/refresh           # トークンリフレッシュ  
GET  /api/auth/me                # ユーザー情報取得
PUT  /api/auth/me                # ユーザー情報更新（パスワード変更等）
DELETE /api/auth/logout          # ログアウト
```

#### 既存エンドポイントの変更
```python
# 全て認証必須に変更
GET  /api/conversations          # ログインユーザーの会話一覧
POST /api/conversations          # 会話作成（user_idは自動設定）
GET  /api/conversations/{id}     # 会話詳細（所有者チェック）
DELETE /api/conversations/{id}   # 会話削除（所有者チェック）

# WebSocket
/ws/{conversation_id}            # 認証チェック必須
```

### 認証ミドルウェア
```python
# JWT検証用の依存関数
async def get_current_user(token: str = Depends(oauth2_scheme))
async def get_current_active_user(current_user: User = Depends(get_current_user))

# 全てのエンドポイントで認証必須（オプション認証は削除）
```

### データモデル
```python
class User(Base):
    __tablename__ = "users"
    id = Column(String, primary_key=True, default=lambda: str(uuid.uuid4()))
    email = Column(String, unique=True, nullable=False)
    username = Column(String, unique=True, nullable=False)
    password_hash = Column(String, nullable=False)
    is_active = Column(Boolean, default=True)
    created_at = Column(DateTime, default=datetime.utcnow)
    updated_at = Column(DateTime, default=datetime.utcnow, onupdate=datetime.utcnow)
    
    # リレーション
    conversations = relationship("Conversation", back_populates="user")

# Conversationモデルに追加
class Conversation(Base):
    # 既存のフィールド...
    user_id = Column(String, ForeignKey("users.id"), nullable=False)
    user = relationship("User", back_populates="conversations")
```

### Pydanticスキーマ
```python
# ユーザー登録はスクリプトで行うため、UserCreateは管理スクリプト用
class UserResponse(BaseModel):
    id: str
    email: str
    username: str
    is_active: bool
    created_at: datetime

class Token(BaseModel):
    access_token: str
    token_type: str = "bearer"
    expires_in: int

class LoginRequest(BaseModel):
    email: str
    password: str

class UserUpdateRequest(BaseModel):
    username: Optional[str] = None
    current_password: Optional[str] = None
    new_password: Optional[str] = None
```

## 4. フロントエンド設計

### 状態管理
```typescript
// 認証コンテキスト
interface AuthState {
  user: User | null;
  token: string | null;
  isAuthenticated: boolean;
  isLoading: boolean;
}

interface User {
  id: string;
  email: string;
  username: string;
  is_active: boolean;
  created_at: string;
}
```

### 新規コンポーネント
```typescript
// 認証関連コンポーネント
- LoginForm.tsx           // ログインフォーム
- UserProfile.tsx        // ユーザープロフィール
- ProtectedRoute.tsx     // 認証が必要なルート保護（全ルート）

// 既存コンポーネントの拡張
- App.tsx                // 認証状態管理の追加
- Sidebar.tsx            // ユーザー情報表示エリア追加
```

### ルーティング
```typescript
// 認証機能
/login                   // ログインページ（未認証時のみ）

// 認証後の機能（全て認証必須）
/                        // メインチャット画面
/profile                 // プロフィール設定
/conversations           // 自分の会話一覧
/settings               // 設定ページ
```

### API通信の変更
```typescript
// axios interceptorsでJWTトークン自動付与
axios.defaults.headers.common['Authorization'] = `Bearer ${token}`;

// トークン期限切れ時の自動リフレッシュ
// エラーハンドリングの統一化
```

## 5. 実装順序

### Phase 1: バックエンド基盤
1. **依存関係追加**: JWT、bcrypt関連ライブラリ（uv add、バージョン指定なし）
2. **データベース**: Userモデル作成、マイグレーション
3. **認証ロジック**: JWT生成・検証、パスワードハッシュ化
4. **認証エンドポイント**: login、refresh、me、logout
5. **ユーザー管理スクリプト**: manage_users.py（サブコマンド: list、register、activate、deactivate、delete）

### Phase 2: 既存機能の認証対応
1. **Conversationモデル拡張**: user_id追加（NOT NULL）
2. **既存エンドポイント**: 全て認証必須に変更
3. **WebSocket認証**: トークン検証必須
4. **データ分離**: ユーザー別会話表示

### Phase 3: フロントエンド実装
1. **認証コンテキスト**: 状態管理システム
2. **ログインフォーム**: ログインUI
3. **既存画面の拡張**: サイドバー、プロフィール表示
4. **ルーティング**: 全ページで認証必須化

### Phase 4: セキュリティ強化・テスト
1. **セキュリティ**: CORS、レート制限、入力検証
2. **エラーハンドリング**: 統一的なエラー処理
3. **テスト**: 認証フロー、API、UI
4. **ドキュメント**: API仕様、セットアップ手順更新
5. **管理スクリプト**: ユーザー管理用CLIツール

## 6. ユーザー管理スクリプト

### 管理スクリプトの仕様
```bash
# backend/scripts/manage_users.py
# サブコマンド形式のユーザー管理スクリプト

python scripts/manage_users.py list                          # ユーザー一覧表示
python scripts/manage_users.py register                      # 対話形式でユーザー登録
python scripts/manage_users.py activate user@example.com     # ユーザー有効化
python scripts/manage_users.py deactivate user@example.com   # ユーザー無効化
python scripts/manage_users.py delete user@example.com       # ユーザー削除
```

### サブコマンド詳細
- **list**: 全ユーザーの情報一覧を表形式で表示（ID、メール、ユーザー名、ステータス、作成日）
- **register**: 対話形式でメール、ユーザー名、パスワードを入力してユーザー登録
- **activate**: 指定したメールアドレスのユーザーを有効化（is_active = True）
- **deactivate**: 指定したメールアドレスのユーザーを無効化（is_active = False）
- **delete**: 指定したメールアドレスのユーザーとその関連データを完全削除

### データ移行戦略
- 既存のconversationsデータは削除または管理者ユーザーに移行
- 認証なしでのアクセスは全て無効化
- データベース整合性チェック機能

## 7. セキュリティ考慮事項

### パスワード管理
- bcryptでハッシュ化（コスト係数12）
- パスワード強度の検証
- パスワードリセット機能（将来実装）

### JWT管理
- 短期間の有効期限（24時間）
- リフレッシュトークンの実装
- トークンのブラックリスト機能（将来実装）

### API保護
- レート制限の実装
- 入力値の検証とサニタイズ
- SQLインジェクション対策

## 8. 環境変数追加

```bash
# backend/.env に追加
JWT_SECRET_KEY=your-secret-key-here
JWT_ALGORITHM=HS256
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440  # 24時間
```

## 9. テスト計画

### バックエンドテスト
- 認証API（register、login、refresh）
- 認証が必要なエンドポイントのアクセス制御
- JWT生成・検証の正確性
- WebSocket認証機能

### フロントエンドテスト
- 認証フローのE2Eテスト
- トークン期限切れ時の挙動
- ゲストモードから認証モードへの移行
- 認証状態の永続化

### セキュリティテスト
- 不正なJWTトークンでのアクセス試行
- パスワード強度の検証
- CORS設定の確認
- XSS、CSRF対策の検証